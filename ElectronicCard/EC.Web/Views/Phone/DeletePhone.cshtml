@model EC.Entities.Entities.Phone

<div class="modal fade" id="delete_@Model.PhoneNumber.GetHashCode()" role="dialog" aria-labelledby="title">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header text-center">
                <h4 class="modal-title text-info" id="title">Удаление телефона</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("DeletePhone", "Phone", new { id = Model.Id }, FormMethod.Post, new { @class = "form-group" }))
                {
                    <div>
                        Вы действительно хотите удалить телефон @Model.PhoneNumber?
                    </div>

                    <div class="modal-footer">
                        <button type="submit" class="btn btn-danger">Удалить</button>
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Закрыть</button>
                    </div>
                }
            </div>
        </div>
    </div>
</div>